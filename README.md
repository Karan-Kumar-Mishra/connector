# connector
# installation
```bash
apt update -y
apt upgrade -y
apt install gcc -y
apt install g++ -y && apt install c++
git clone https://github.com/Karan-Kumar-Mishra/connector.git
cd connector
bash setup.sh
```
